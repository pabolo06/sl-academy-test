{
    "name": "sl-academy-deploy",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build && cp -r out ../",
        "start": "node backend/index.ts"
    },
    "dependencies": {
        "@appdeploy/sdk": "latest",
        "@appdeploy/client": "latest",
        "@supabase/supabase-js": "^2.39.7"
    }
}